apiVersion: v1
kind: Service
metadata:
  name: coffee-shop
  namespace: coffee-shop
  labels:
    app.kubernetes.io/name: coffee-shop
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: coffee-shop
  ports:
    - name: http
      port: 3000
      targetPort: http
      nodePort: 30080
